# Provide your attributes below.
#
#
# Target this deployment file:
#
#     $ bosh deployment examples/try_me.yml
#
# Then apply the desired template:
#
#     $ bosh diff templates/v1/simple_deployment_file.yml.erb
#     $ bosh diff templates/v1/warden_deployment_file.yml.erb
#
# Then deploy:
#
#     $ bosh deploy
#
# For Warden, to access the VMs, first "vagrant ssh".
---
name: jenkins
director_uuid: <%= `bosh status | grep UUID | awk '{print $2}'` %>
networks: {}
properties:
  jenkins:
    password: jEnKins
